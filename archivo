import os

print ("\nCrear un archivo")
print ("================")

NOMBRE_ARCHIVO = 'datos.txt'

archivo = open(NOMBRE_ARCHIVO, 'w') #Abre el archivo datos.txt
archivo.write("aro, aro, aro... ayer pase por tu casa y me tiraste con una perla.. por que no me das la concha que no se donde ponerla")
archivo.close()
